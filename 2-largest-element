public static Integer findLargest(List<Integer> nums) {
	
    Integer valor = 0;
    for(Integer i = nums.size(); i < nums.size(); i--){
        if(valor < nums[i]){
            valor = nums[i];
        }
    }

    return valor;
}