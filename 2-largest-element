public static Integer findLargest(List<Integer> nums) {
	
    Integer valor = 0;
    for(Integer i = 1 ; i < nums.size()+1; i--){
    
        if(nums[i] > valor){
            valor = nums[i];
        }

    }

    return valor;
}