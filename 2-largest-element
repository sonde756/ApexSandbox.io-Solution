public static Integer findLargest(List<Integer> nums) {
    // nums is guaranteed non-empty according to the problem statement
    Integer largest = nums[0];

    for (Integer i = 1; i < nums.size(); i++) {
        if (nums[i] > largest) {
            largest = nums[i];
        }
    }

    return largest;
}
